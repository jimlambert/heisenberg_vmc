#include <cstdlib>
#include <time.h>
#include <iostream>
#include <Eigen/Dense>
#include "VarParam.h"
#include "Simulation.h"
#include "LocalMeasurement.h"
#include "BCSHamiltonian.h"

using namespace std;

int main() {
  //size_t L=12;
  //int space=0;
  //size_t equil=100000;
  //size_t simul=100000;
  //size_t vstep=100;
  //VMC::ParamList_t params;
  //VMC::BCSVarParam onsite(0.0, space, VMC::Onsite, 2*L);
  //VMC::BCSVarParam nnhopp(2.0, 1, VMC::Hopping, 2*L);
  //VMC::BCSVarParam nnpair(-2.0, 1, VMC::Pairing, 2*L);
  //params.push_back(onsite);
  //params.push_back(nnhopp);
  //params.push_back(nnpair);
  //VMC::HeisChainSim simulation(L, params);
  //simulation.optimize(vstep, equil, simul);
  return 0;
}
